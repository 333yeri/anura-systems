{
  "name": "anura-systems",
  "version": "1.0.0",
  "description": "Anura Systems — single-page 3D cinematic experience",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 9090",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 9090"
  },
  "dependencies": {
    "gsap": "^3.12.5",
    "three": "^0.160.0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}